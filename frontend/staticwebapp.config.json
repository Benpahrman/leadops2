{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/*.{css,js,svg,png,jpg,jpeg,gif,ico,json,map,txt,woff,woff2}"]
  },
  "globalHeaders": {
    "X-Content-Type-Options": "nosniff",
    "Referrer-Policy": "strict-origin-when-cross-origin"
  }
}
